export default function AgentRunPage() {
  return (
    <div className="rounded-[32px] border border-dashed border-black/10 bg-white p-10 text-center shadow-sm">
      <h1 className="text-3xl font-semibold text-slate-950">Run view placeholder</h1>
      <p className="mx-auto mt-4 max-w-2xl text-sm leading-6 text-slate-600">
        This route exists so the shell and demo flow have somewhere stable to point while Person 3
        lands the execution UI.
      </p>
    </div>
  );
}
